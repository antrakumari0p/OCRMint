/**
 * Shared, cross-cutting types for OCRMint live here (API models, domain
 * entities, shared prop shapes). Nothing product-specific exists yet —
 * this file is a placeholder so imports like `@/types` resolve cleanly
 * as features are added.
 */

export {};
